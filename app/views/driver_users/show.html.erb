<%= render "users/partials/show_css" %>
<div class="container">
  <div class="user-profile">
    <div class="profile-header-background"><img src="http://demo.thedevelovers.com/dashboard/queenadmin-1.2/assets/img/city.jpg" alt="Profile Header Background"></div>
    <div class="row">
      <div class="col-md-4">
        <div class="profile-info-left">
          <div class="text-center">
            <img src="http://bootdey.com/img/Content/avatar/avatar1.png" alt="Avatar" class="avatar img-circle">
            <h2><%= @driver.full_name %> <small>(<%= @driver.type_of_user %>)</small></h2>
          </div>
          <div class="action-buttons">
            <div class="row">
              <div class="col-xs-6">
                <a href="#" class="btn btn-success btn-block"><i class="fa fa-plus-round"></i> Follow</a>
              </div>
              <div class="col-xs-6">
                <a href="#" class="btn btn-primary btn-block"><i class="fa fa-android-mail"></i> Message</a>
              </div>
            </div>
          </div>
          <div class="section">
            <h3>About Me</h3>
            <p></p>  
          </div>
          <div class="section">
            <h3>Load Statistics</h3>
            <p>
             

              <span class="badge">
                <%= @live_loads.count if @driver.type == "DriverUser" %>
              </span> Live Loads
            </p>
            <p>
              
              <span class="badge">
                <%= @completed_loads.count if @driver.type == "DriverUser"  %>
              </span> Completed Loads
            </p>

            <p><span class="badge">620</span> Likes</p>
          </div>
          <div class="section">
            <h3>Social</h3>
            <ul class="list-unstyled list-social">
              <li><i class="fa fa-paper-plane"></i> <%= @driver.email %></li>
              <li><i class="fa fa-mobile-phone"></i> <%= @driver.cellphone %></li>
              <li>
                <% if @driver.telephone.present? %>
                <i class="fa fa-phone"></i> <%= @driver.telephone %>
                <% end %>
                <% if @driver.extention.present? %>
                ext. <%= @driver.extention %>
                <% end %>
                </li>
               <div>
                <strong>Emergency Contact</strong>
              <li><i class="fa fa-heart-o"></i> <%= @driver.emergency_contact %></li>
              <li><i class="fa fa-user-md"></i> <%= @driver.emergency_contact_number %></li>
              </div>
              <hr>
              <div>
                <strong>Home Address</strong>
              <address>
                <%= @driver.street %><br />
                <%= @driver.city %><br />
                <%= @driver.state %> 
                <%= @driver.zip %>
              </address>
      </div>
       
            </ul>
          </div>
        </div>
      </div>
      <div class="col-md-8">
        <div class="profile-info-right">
          <ul class="nav nav-pills nav-pills-custom-minimal custom-minimal-bottom">
            <li><a href="#LiveLoads" data-toggle="tab">Live Loads</a></li>
            <li><a href="#CompletedLoads" data-toggle="tab">Completed Loads</a></li>
            <li><a href="#Maps" data-toggle="tab">Maps</a></li>
            <li><a href="#Chat" data-toggle="tab">Chat</a></li> 
          </ul>
          <div class="tab-content">
            <div class="tab-pane fade" id="LiveLoads">
            <%= render "live_loads" %>
            </div>
            <div class="tab-pane fade" id="CompletedLoads">
            <%= render "completed_loads" %>
            </div>
            <div class="tab-pane fade" id="Maps">
            <%= render "maps" %>
            </div>
            <div class="tab-pane fade" id="Chat">
            <%= render "chat" %> 
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%= link_to "Edit", edit_user_path(@driver), class: "btn btn-hrcprimary btn-xs" %>
<%= link_to "All Users", users_path, class: "btn btn-hrcsuccess btn-xs" %>


<% @completed_loads.each do |cl| %>
<%= cl.origin_city %>
<% end %>